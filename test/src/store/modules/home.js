import {} from '../../serve/index'

const state={
    list=[]
}

const mutions={
    
}

const action ={
    // async getTimeline(context,payload){

    // }
}

export default {
    namespaced:true,
    state,
    mutations,
    action
}